{
  "name": "campus-timetable",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "data:build": "python tools/parse_timetables.py --input raw --output public/data --clean && python tools/parse_language_timetables.py --input \"raw/Timetable_2026Autumn_ English and German.xlsx\" --output public/data",
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "lint": "eslint src vite.config.ts",
    "test": "vitest run && python tools/test_parsers.py",
    "check": "pnpm lint && pnpm test && pnpm build",
    "pages:deploy": "pnpm check && wrangler pages deploy dist --project-name campus-timetable",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "eslint": "^10.9.1",
    "eslint-plugin-vue": "^10.10.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.68.0",
    "vite": "^6.0.5",
    "vitest": "^4.1.11",
    "vue-eslint-parser": "^10.4.1",
    "vue-tsc": "^2.2.0",
    "wrangler": "4.126.0"
  }
}
